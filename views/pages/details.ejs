<!DOCTYPE html>
<html lang="en">

<head>
  <%- include("../partials/header") %>
</head>

<body class="bg-gray-100 text-gray-800">
  <div class="container mx-auto p-5 flex justify-center">
    <div class="bg-red-200 border-4 border-red-200 rounded-lg shadow-xl p-5 w-80 text-center">
      <h1 class="text-3xl font-bold mb-3 capitalize text-red-700">
        <%= pokemon.name %>
      </h1>
      <div class="bg-white border-2 border-gray-300 rounded-lg p-3 flex flex-col items-center">
        <img src="<%= pokemon.sprites.front_default %>" alt="<%= pokemon.name %>"
          class="w-40 h-40 mb-3 border-2 border-gray-400 rounded-lg" />
          <div class="flex flex-wrap justify-center mt-2">
            <div class="flex space-x-2">
              <% pokemon.types.forEach(t=> { %>
                <span class="px-3 py-1 bg-purple-400 text-white rounded-full text-sm font-semibold">
                  <%= t.type.name %>
                </span>
                <% }) %>
            </div> 
          </div>
        <p class="text-lg font-semibold text-gray-800">Height: <%= pokemon.height %>
        </p>
        <p class="text-lg font-semibold text-gray-800">Weight: <%= pokemon.weight %>
        </p>
      </div>
      <div class="mt-5">
        <a href="/" class="px-4 py-2 bg-red-500 text-white rounded-lg hover:bg-red-600">Back</a>
      </div>
    </div>
  </div>
  <%- include("../partials/footer") %>
</body>

</html>